<h3>Add new Story</h3>
  <%= form_for(@story, :html => {class: "form", role: "form"}) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, class: "form-control", autofocus: true %>
  <%= f.label :body %>
  <%= f.text_area :body, rows: 10, class: "form-control" %>
  <%= f.submit class: "button" %>
<% end %>

